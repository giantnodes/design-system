name: '🏗️  '
author: 'PHILLIPS71'

inputs:
  BUILD_COMMAND:
    default: 'build'
    required: true

runs:
  using: 'composite'
  steps:
    - name: '🏗️  Build'
      shell: bash
      run: |
        pnpm run ${{ inputs.BUILD_COMMAND }}
